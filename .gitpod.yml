image:
  file: .gitpod.Dockerfile
tasks:
  - init: |
      npm install
      cabal update
      stack build

    command: |
      npm start
ports:
  - port: 3000
    onOpen: notify
  - port: 3001
    onOpen: ignore

vscode:
  extensions:
    - haskell.haskell
    - elmTooling.elm-ls-vscode
